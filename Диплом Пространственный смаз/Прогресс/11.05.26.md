Задачи:
- Перевод изображения в оттенки серого;
- БПФ, модуль;
- Задаться ядром линейного смаза - встроенными средствами или самостоятельно;
- Взять его БПФ;
- Умножение в частотной области;
- ОБПФ;
- Визуализация результата.

<u>Динамический диапазон яркостей большой</u> (например, абсолютная амплитуда постояной составляющей и малые амплитуды высокочастотных компонент - разница может быть в несколько порядков) - <u>используем логарифмическую шкалу: она сжимает этот диапазон</u>, так как с ростом **x** меняется все медленнее, а при малых x - относительно быстро.

- Переписана утилита show_image - теперь автоматически определяет, цветное ли трехканальное или иное изображение, и обработка только через matplotlib:

``` python
def show_image(img, title="Image"):
    """Показывает изображение. use_matplotlib=False → cv2.imshow"""
    # обработка теперь только через matplotlib
    if img is None:
        return
    # утилита сама определяет, что посылаем на вход:
    # три канала и uint_8 - обычные цветные изображения: конвертация BGR -> RGB
    # в оттенках серого или любые одноканальные (амплитудный спектр, e.g.) - вывод как есть
    # img.shape - кортеж, в котором хранятся размерности numpy-массива
    # при загрузке картинки через cv2.imread() - 3 числа: высота и ширина в пикселях
    # и глубина в цветовых каналах.
    # img.shape[2] - показывает количество цветовых каналов
    if len(img.shape) == 3 and img.shape[2] == 3 and img.dtype == np.uint8:
        # RGB-фото
        img_rgb = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
        plt.imshow(img_rgb)
    else:
        # в оттенках серого (2D), спектр или float - что-то другое
        # явное задание цветовой схемы cmap через тернарный оператор
        cmap = 'gray' if len(img.shape) == 2 else None
        plt.imshow(img, cmap = cmap)
    plt.title(title)
    plt.axis("off")
    plt.tight_layout()
    plt.show()
```

- Изменена утилита load_image: теперь при загрузке один раз BGR конвертируется в RGB:
``` python
def load_image(filepath):
    """читает файл с диска и возвращает файл с картинкой."""
# превращение строки пути в удобный формат
    filepath = Path(filepath)
# imread требует строку
    img = cv2.imread(str(filepath))
    if img is None:
        print(f"Ошибка: не удалось прочитать {filepath}")
        return None
# сразу конвертация из BGR в RGB
    img_rgb = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
    return img_rgb
```

Сразу же подправлена утилита show_image:
``` python
def show_image(img_rgb, title="Image"):

    """Показывает изображение"""
    # обработка теперь только через matplotlib
    if img_rgb is None:
        return
    # img.shape - кортеж, в котором хранятся размерности numpy-массива
    # при загрузке картинки через cv2.imread() - 3 числа: высота и ширина в пикселях
    # и глубина в цветовых каналах.
    # img.shape[2] - показывает количество цветовых каналов в этом случае
    if len(img_rgb.shape) == 2:
        # в оттенках серого (2D), спектр - что-то другое
        # явное задание цветовой схемы cmap через тернарный оператор
        cmap = 'gray' if len(img_rgb.shape) == 2 else None
        plt.imshow(img_rgb, cmap = cmap)
    else:
        # RGB-фото
        plt.imshow(img_rgb)
    plt.title(title)
    plt.axis("off")
    plt.tight_layout()
    plt.show()
```
- Получен модуль БПФ исходного изображения в оттенках серого:
Само изображение:
